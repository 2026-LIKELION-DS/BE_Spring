spring:
  datasource:
    url: jdbc:mysql://springboot-mysql:3306/projectname_db?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Seoul&characterEncoding=utf8
    username: admin
    password: admin1234!
  jpa:
    hibernate.ddl-auto: update
    show-sql: true
    properties.hibernate.format_sql: true # SQL 콘솔 출력을 예쁘게 줄바꿈해주는 설정
server.port: 8080